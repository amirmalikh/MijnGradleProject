apply plugin: 'java'
apply plugin: 'application'

mainClassName = "fraction.BreukMain"
run.args = ["Winston", "bla", "17"]
run {
    standardInput = System.in       // voor Scanner
}

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

